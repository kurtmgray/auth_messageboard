extends layout 

block content
    h1= title

    form(action="/log-in" method="POST"_)
        label(for="username">Username)
        input(name="username" placeholder="username" type="text")
        label(for="password">Password)
        input(name="password" type="password")
        button(type="submit")= "Log In"
       